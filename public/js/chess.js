export default class Chess {
  constructor(x, y, radius, color, row, col) {
    this.x = x; // X position on the screen
    this.y = y; // Y position on the screen
    this.color = color;
    this.radius = radius;
    this.row = row;
    this.col = col;
  }
}
