<div class="messages list-group" auto-scroll-to-bottom>
    <div class="list-group-item message" ng-repeat="message in messages track by $index">
        某某：{{message}}
    </div>
</div>
<form class="message-creator" ng-controller="MessageCreatorCtrl">
    <div class="form-group">
        <textarea name="sendMsgArea" id="sendMsgArea" cols="30" rows="4" class="form-control message-input" required=""
            ng-model="newMessage" ctrl-enter-break-line="createMessage()"
            placeholder="Ctrl + Enter to quick send"></textarea>
    </div>
</form>